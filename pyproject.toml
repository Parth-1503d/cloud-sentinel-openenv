[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cloud_sentinel"
version = "1.0.0"
description = "OpenEnv Cloud Security Audit Environment"
authors = [
    {name = "Your Name", email = "student@college.edu"}
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "pillow"
]